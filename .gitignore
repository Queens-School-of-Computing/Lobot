# vi swap files
*.sw[a-p]

# python virtual env
.venv

# Local session memory file
tools/LOBOT-MEMORY.md

# Local IDE files
.DS_Store
.vscode.desktop
tools/OLD-cluster-setup.html

# Test run logs (timestamped, one per run)
tools/tests/run-*.log

# Local development venv (for running tests without deploying to the dev server)
.venv-dev/
