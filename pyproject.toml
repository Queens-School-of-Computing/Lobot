[tool.ruff]
line-length = 100
target-version = "py314"

# Enable all pycodestyle & Pyflakes rules
[tool.ruff.lint]
select = ["E", "F"]

[tool.ruff.format]
preview = true
quote-style = "preserve"
skip-magic-trailing-comma = false
